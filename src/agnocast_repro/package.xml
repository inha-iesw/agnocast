<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>agnocast_repro</name>
  <version>0.1.0</version>
  <description>
    Burst node for the Agnocast shared-memory physical-page reclaim regression test:
    it drove the leak before the fix and now confirms freed blocks are returned to the
    kernel via the deallocate-time madvise(MADV_REMOVE).
  </description>
  <maintainer email="Mcleroysane19@inha.edu">repro</maintainer>
  <license>Apache License 2.0</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>rclcpp</depend>
  <depend>agnocastlib</depend>
  <depend>agnocast_sample_interfaces</depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
